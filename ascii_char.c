#include <stdio.h>
int main()
{
	/* int i;
	for (i = -128;i<=-1;i++)
	{
	printf("%d corresponds to **%c**\n",i,i);
	}
	//irintf("%d corresponds to **%c**\n",i,i);
	}
	*/
int	a=1;
printf("%ld", sizeof(1));
	
	return 0;
}
